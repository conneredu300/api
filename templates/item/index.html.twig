{% extends 'base.html.twig' %}

{% block title %}Item!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        {#{% for item in content %}#}
            {#<p>Nome: {{ item.personname }}</p>#}
            {#<p>Id: {{ item.personid }}</p>#}
            {#<ul>#}
                {#{% for phone in item.phones %}#}
                    {#<li>Phone: {{ phone.phone }}</li>#}
                {#{% endfor %}#}
            {#</ul>#}
        {#{% endfor %}#}

        {% for i in content %}
            {{ i.shipto.name }}
        {% endfor %}
    </div>
{% endblock %}
